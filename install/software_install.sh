#!/usr/bin/env bash

#intall general shit (must have list)
sudo apt-get install -y curl zsh vim tree nmap tmux git figlet fonts-inconsolata rsync zsh

# Make ZSH to be the  default Shell
#chsh -s /bin/zsh

# Installing Oh My ZSH via wget
#sh -c "$(wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -O -)"

# powerlevel9k theme
#git clone https://github.com/bhilburn/powerlevel9k.git ~/.oh-my-zsh/custom/themes/powerlevel9k

#bash
sudo apt-get install bash bash-completion
